# =============================================================================
# WORKFLOW: Deploy to Self-Hosted VM
# =============================================================================
# Este workflow despliega la aplicación TeachingPlanner en una VM con runner
# auto-hospedado (self-hosted runner).
#
# FLUJO DE EJECUCIÓN:
#   1. unit-tests           → Ejecuta tests unitarios
#   2. e2e-tests            → Ejecuta tests end-to-end (depende de unit-tests)
#   3. build-and-push       → Construye y sube imágenes a GHCR (depende de e2e-tests)
#   4. deploy               → Despliega en VM self-hosted (depende de build-and-push)
#
# TRIGGERS:
#   - Push a la rama 'main'
#   - Ejecución manual desde GitHub (workflow_dispatch)
#
# SECRETS REQUERIDOS:
#   - SSL_CERT              : Certificado SSL (cert.pem)
#   - SSL_KEY               : Clave privada SSL (key.pem)
#   - GITHUB_TOKEN          : Token automático de GitHub (para GHCR)
#
# VARIABLES REQUERIDAS:
#   - REPOSITORY_NAME       : Nombre del repositorio en formato owner/repo
#
# REQUISITOS DEL RUNNER:
#   - Docker y Docker Compose instalados
#   - GitHub Actions Runner configurado y en ejecución
#   - Acceso a internet para descargar imágenes de GHCR
# =============================================================================

name: Deploy to Self-Hosted VM

# -----------------------------------------------------------------------------
# TRIGGERS: Cuándo se ejecuta este workflow
# -----------------------------------------------------------------------------
on:
  push:
    branches:
      - main  # Despliegue automático en cada push a main
  workflow_dispatch:  # Permite ejecución manual desde la UI de GitHub

# -----------------------------------------------------------------------------
# JOBS: Definición de trabajos
# -----------------------------------------------------------------------------
jobs:

  # ===========================================================================
  # JOB 1: TESTS UNITARIOS
  # ===========================================================================
  # Ejecuta las pruebas unitarias de todos los servicios
  # ---------------------------------------------------------------------------
  unit-tests:
    name: 🧪 Tests Unitarios
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Clonar el repositorio
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: 📦 Instalar dependencias
        run: |
          npm --prefix gateway_service ci
          npm --prefix planner_service ci
          npm --prefix auth_service ci
          npm --prefix user_service ci
          npm --prefix webapp ci

      - name: 🧪 Ejecutar Tests Unitarios
        run: |
          npm --prefix planner_service test -- --coverage

  # ===========================================================================
  # JOB 2: TESTS END-TO-END
  # ===========================================================================
  # Ejecuta las pruebas de integración/E2E
  # Requiere: unit-tests
  # ---------------------------------------------------------------------------
  e2e-tests:
    name: 🔗 Tests E2E
    needs: [unit-tests]
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      mariadb:
        image: mariadb:11
        env:
          MYSQL_ROOT_PASSWORD: testpassword
          MYSQL_DATABASE: planner_db_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mariadb-admin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: 📥 Clonar el repositorio
        uses: actions/checkout@v5

      - name: ⚙️ Configurar Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: 📦 Instalar dependencias
        run: |
          npm --prefix gateway_service ci
          npm --prefix planner_service ci
          npm --prefix auth_service ci
          npm --prefix user_service ci
          npm --prefix webapp ci

      - name: 🗄️ Crear base de datos management_db_test
        run: |
          mysql -h 127.0.0.1 -u root -ptestpassword -e "CREATE DATABASE IF NOT EXISTS management_db_test;"

      - name: 🔧 Copiar archivo de configuración CI
        run: cp .env.ci .env

      - name: 🏗️ Compilar servicios backend
        run: |
          npm --prefix gateway_service run build
          npm --prefix planner_service run build
          npm --prefix auth_service run build
          npm --prefix user_service run build

      - name: 🚀 Levantar servicios en background
        run: |
          (cd gateway_service && node --env-file=../.env dist/index.js > ../gateway.log 2>&1) &
          echo $! > gateway.pid
          (cd planner_service && node --env-file=../.env dist/index.js > ../planner.log 2>&1) &
          echo $! > planner.pid
          (cd auth_service && node --env-file=../.env dist/index.js > ../auth.log 2>&1) &
          echo $! > auth.pid
          (cd user_service && node --env-file=../.env dist/index.js > ../user.log 2>&1) &
          echo $! > user.pid

      - name: 🐛 Verificar que los servicios se iniciaron
        run: |
          echo "=== PIDs de los servicios ==="
          cat gateway.pid planner.pid auth.pid user.pid || echo "Error: No se encontraron PIDs"
          echo ""
          echo "=== Procesos activos ==="
          ps aux | grep -E "gateway|planner|auth|user" | grep -v grep || echo "No hay procesos activos"
          echo ""
          echo "=== Primeras líneas de logs ==="
          echo "--- Gateway ---"
          head -20 gateway.log || echo "No hay log de gateway"
          echo "--- Planner ---"
          head -20 planner.log || echo "No hay log de planner"
          echo "--- Auth ---"
          head -20 auth.log || echo "No hay log de auth"
          echo "--- User ---"
          head -20 user.log || echo "No hay log de user"

      - name: ⏳ Esperar a que servicios estén listos
        run: npx tsx scripts/wait-for-services.ts
        working-directory: webapp

      - name: 👤 Crear usuario de prueba
        run: npm --prefix user_service run seed:ci

      - name: 🎭 Instalar navegadores Playwright
        run: npx playwright install chromium --with-deps
        working-directory: webapp

      - name: 🏁 Ejecutar Tests E2E
        run: npm run test:e2e:ci
        working-directory: webapp
        env:
          CI: true

      - name: 📊 Subir reporte de Playwright si falla
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: playwright-report
          path: webapp/playwright-report/
          retention-days: 7

      - name: 📸 Subir screenshots y videos si falla
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: webapp/test-results/
          retention-days: 7

      - name: 🧹 Cleanup - Mostrar logs si falla
        if: failure()
        run: |
          echo "=== Gateway Service Logs ==="
          cat gateway.log || true
          echo ""
          echo "=== Planner Service Logs ==="
          cat planner.log || true
          echo ""
          echo "=== Auth Service Logs ==="
          cat auth.log || true
          echo ""
          echo "=== User Service Logs ==="
          cat user.log || true

      - name: 🧹 Cleanup - Matar procesos
        if: always()
        run: |
          kill $(cat gateway.pid) 2>/dev/null || true
          kill $(cat planner.pid) 2>/dev/null || true
          kill $(cat auth.pid) 2>/dev/null || true
          kill $(cat user.pid) 2>/dev/null || true

  # ===========================================================================
  # JOB 3: BUILD & PUSH DOCKER IMAGES
  # ===========================================================================
  # Construye todas las imágenes Docker y las sube a GitHub Container Registry
  # Requiere: e2e-tests
  # ---------------------------------------------------------------------------
  build-and-push-images:
    name: 🐳 Build & Push Images
    needs: [e2e-tests]
    runs-on: ubuntu-latest
    permissions:
      contents: read    # Para clonar el repositorio
      packages: write   # Para subir imágenes a GHCR

    steps:
      # -----------------------------------------------------------------------
      # SETUP: Preparación del entorno
      # -----------------------------------------------------------------------
      - name: 📥 Clonar código fuente
        uses: actions/checkout@v5

      - name: ⚙️ Configurar Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: 🔑 Login en GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # BUILD: Construcción de imágenes (Frontend)
      # -----------------------------------------------------------------------
      - name: 🌐 Build & Push - webapp
        uses: docker/build-push-action@v6
        with:
          context: ./webapp
          file: ./webapp/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/webapp:latest
          cache-from: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/webapp:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/webapp:buildcache,mode=max

      # -----------------------------------------------------------------------
      # BUILD: Construcción de imágenes (API Gateway)
      # -----------------------------------------------------------------------
      - name: 🚪 Build & Push - gateway_service
        uses: docker/build-push-action@v6
        with:
          context: ./gateway_service
          file: ./gateway_service/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/gateway_service:latest
          cache-from: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/gateway_service:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/gateway_service:buildcache,mode=max

      # -----------------------------------------------------------------------
      # BUILD: Construcción de imágenes (Servicios de negocio)
      # -----------------------------------------------------------------------
      - name: 📅 Build & Push - planner_service
        uses: docker/build-push-action@v6
        with:
          context: ./planner_service
          file: ./planner_service/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/planner_service:latest
          cache-from: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/planner_service:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/planner_service:buildcache,mode=max

      - name: 🔐 Build & Push - auth_service
        uses: docker/build-push-action@v6
        with:
          context: ./auth_service
          file: ./auth_service/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/auth_service:latest
          cache-from: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/auth_service:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/auth_service:buildcache,mode=max

      - name: 👤 Build & Push - user_service
        uses: docker/build-push-action@v6
        with:
          context: ./user_service
          file: ./user_service/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/user_service:latest
          cache-from: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/user_service:buildcache
          cache-to: type=registry,ref=ghcr.io/${{ vars.REPOSITORY_NAME }}/user_service:buildcache,mode=max

      # -----------------------------------------------------------------------
      # BUILD: Construcción de imágenes (Bases de datos)
      # -----------------------------------------------------------------------
      - name: 🗄️ Build & Push - planner_database
        uses: docker/build-push-action@v6
        with:
          context: ./planner_database
          file: ./planner_database/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/planner_database:latest
          # Sin cache para DBs (raramente cambian y son pequeñas)

      - name: 🗄️ Build & Push - management_database
        uses: docker/build-push-action@v6
        with:
          context: ./management_database
          file: ./management_database/Dockerfile
          push: true
          tags: ghcr.io/${{ vars.REPOSITORY_NAME }}/management_database:latest
          # Sin cache para DBs (raramente cambian y son pequeñas)

  # ===========================================================================
  # JOB 4: DEPLOY TO SELF-HOSTED VM
  # ===========================================================================
  # Despliega la aplicación directamente en la VM donde corre el runner
  # Requiere: build-and-push-images
  #
  # NOTA: Este job usa 'runs-on: self-hosted' lo que significa que se ejecuta
  # directamente en tu VM, no necesita SSH porque ya está ahí.
  # ---------------------------------------------------------------------------
  deploy:
    name: 🚀 Deploy to Self-Hosted
    needs: [build-and-push-images]
    runs-on: self-hosted  # Se ejecuta en el runner auto-hospedado de la VM

    steps:
      # -----------------------------------------------------------------------
      # SETUP: Preparación del entorno
      # -----------------------------------------------------------------------
      - name: 📥 Clonar código fuente
        uses: actions/checkout@v5

      - name: 🔑 Autenticar en GitHub Container Registry
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # -----------------------------------------------------------------------
      # TRANSFER: Preparar archivos de configuración
      # -----------------------------------------------------------------------
      - name: 📋 Copiar docker-compose al directorio de producción
        run: |
          mkdir -p $HOME/TeachingPlanner
          cp docker-compose.selfhosted.yml $HOME/TeachingPlanner/docker-compose.yml

      # -----------------------------------------------------------------------
      # SSL: Configurar certificados SSL
      # -----------------------------------------------------------------------
      - name: 🔐 Crear certificados SSL desde secrets
        run: |
          # Crear directorio para certificados
          mkdir -p $HOME/TeachingPlanner/certs

          # Escribir certificados desde secrets
          echo "${{ secrets.SSL_CERT }}" > $HOME/TeachingPlanner/certs/cert.pem
          echo "${{ secrets.SSL_KEY }}" > $HOME/TeachingPlanner/certs/key.pem

          # Configurar permisos seguros
          chmod 600 $HOME/TeachingPlanner/certs/key.pem   # Solo lectura para owner
          chmod 644 $HOME/TeachingPlanner/certs/cert.pem  # Lectura para todos

      # -----------------------------------------------------------------------
      # DEPLOY: Desplegar aplicación
      # -----------------------------------------------------------------------
      - name: 🛑 Detener servicios actuales
        run: |
          cd $HOME/TeachingPlanner
          # docker compose down || true

          # ⚠️ ADVERTENCIA: Descomentar la siguiente línea BORRA todos los datos de las bases de datos
          # Útil para desarrollo/TFG cuando se necesita resetear el esquema completamente
          docker compose down -v || true

      - name: 🗑️ Limpiar imágenes Docker no utilizadas
        run: |
          docker image prune -f

      - name: 📥 Descargar últimas imágenes desde GHCR
        run: |
          cd $HOME/TeachingPlanner
          docker compose pull

      - name: 🚀 Iniciar servicios con las nuevas imágenes
        run: |
          cd $HOME/TeachingPlanner
          docker compose up -d --force-recreate --remove-orphans

      # -----------------------------------------------------------------------
      # VERIFICACIÓN: Comprobar estado del despliegue
      # -----------------------------------------------------------------------
      - name: ⏳ Esperar a que los servicios estén listos
        run: |
          echo "Esperando 10 segundos para que los servicios inicien..."
          sleep 10

      - name: ✅ Verificar estado de los servicios
        run: |
          cd $HOME/TeachingPlanner
          docker compose ps

      - name: 📋 Mostrar logs recientes (debug)
        if: always()  # Se ejecuta siempre, incluso si fallan pasos anteriores
        run: |
          cd $HOME/TeachingPlanner
          docker compose logs --tail=50
